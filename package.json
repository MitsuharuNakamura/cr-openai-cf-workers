{
  "name": "conversationrelaywss",
  "version": "1.0.0",
  "description": "Cloudflare Workers implementation of Twilio ConversationRelay WebSocket server",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["cloudflare", "workers", "websocket", "twilio", "openai"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
